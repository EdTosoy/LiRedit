import React, { ReactElement } from 'react'

interface Props {
    
}

export default function register({}: Props): ReactElement {
    return (
        <div>
            register page
        </div>
    )
}
